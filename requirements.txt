# 核心依赖（所有脚本共用）
openai>=1.10.0
python-dotenv>=1.0.0
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0
jieba>=0.42.1

# 模型与机器学习（llm_annotation.py, model_validation.py）
scikit-learn>=1.3.0
sentence-transformers>=2.2.0
transformers>=4.35.0
torch>=2.0.0

# 文本处理与工具（semantic_completion_pipeline.py）
pypinyin>=0.50.0
emoji>=2.8.0          # 用于提取emoji含义

# 可选依赖（特定增强或回退方案）
# 数据增强（data_augmentation.py）离线回译
argostranslate>=1.9.0

# 模型加载 fallback（model_validation.py 中尝试使用）
modelscope>=1.9.0
